




























































































































































       -###
       -###
       -###
           Adds an implicit #define into the predefines buffer which is read before the source file is preprocessed.
           Adds an implicit #define into the predefines buffer which is read before the source file is preprocessed.
           Adds an implicit #define into the predefines buffer which is r           whole program optimization is done at link time. -O1 is somewhere between -O0 and -O2.
           whole program optimization is done at link time. -O-315095848348 is somewhere between -O0 and -O2.
           whole program optimization is done at link time. -O1 is somewhere between -O0 and -O2.
           whole program optimization is done at link time. -O-315095848348 is somewhere between -O-1 and -O2.
           whole program optimization is done at link time. -O1 is somewhere between -O1 and -O2.
       -Wl,args
       -Wl,args
       -Wl,args
       -Wp,args
       -Wp,args
       -Wp,args
           Write output to file.
           Write output to file.
           Write output to file.
         -Wwarnings... -pedantic
         -Wwarnings... -pedantic
         -Wwarnings... -p-Xpreprocessor arg
